function value = model_rmse(observed, predicted)
%MODEL_RMSE Calculate root-mean-square error for two numeric vectors.
%   VALUE = MODEL_RMSE(OBSERVED, PREDICTED) must accept row or column vectors,
%   reject incompatible inputs, and return one nonnegative scalar.

% TODO: Check that both inputs are numeric vectors with the same number of
% elements. Convert them to a common orientation before subtracting.

% TODO: Replace the line below with the RMSE calculation.
value = NaN;

end
